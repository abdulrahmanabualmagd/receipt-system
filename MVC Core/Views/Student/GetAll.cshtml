@model IEnumerable<Student>

<div class="d-flex flex-wrap container">
    @foreach(Student item in Model)
    {
        <button class="btn btn-primary m-1 py-2 px-2 fs-4">@item.Name</button>
    }
</div>